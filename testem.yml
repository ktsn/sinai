---
  framework: mocha
  src_files:
    - .tmp/test.js
  launch_in_dev:
    - Chrome
  launch_in_ci:
    - Chrome
  browser_args:
    Chrome:
      - --headless
      - --disable-gpu
      - --no-sandbox
      - --remote-debugging-port=9222